const getMaxValueIndices = (arr) => {
  let max = -Infinity, indices = [];
  for (let i = 0; i < arr.length; ++i){
    if (arr[i] < max) continue;
    if (arr[i] > max) {
      indices = [];
      max = arr[i];
    }
    indices.push(i);
  }
  return indices;
}

export default getMaxValueIndices;

//# sourceMappingURL=getMaxValueIndices.min.js.map
